<div class="l-page">
  {{> organisms-header }}
  <main class="l-main">

    <div class="l-container">
      {{> molecules-page-title }}
      {{> molecules-breadcrumbs }}
    </div>

    <div class="l-container l-basic">

      <div class="l-content">
        <div class="panel o-box">
          <!-- Featured Image -->
          {{> atoms-landscape-16x9 }}

          <!-- Event date -->
          <h2>{{ event.date }} @ {{ event.time }}</h2>
          <div>{{ location }}</div>
          <div><a href="{{ url }}">Map to Event</a></div>
          <div><a href="{{ url }}">{{ addToCalendar }}</a></div>

          {{> molecules-social-share }}

          <div class="u-clearfix">
            {{> organisms-event-body }}
          </div>

          <h2 class="heading--auxiliary">Documents</h2>
          {{> atoms-list-download }}

          {{# singleCategory }}
            <h2 class="heading--auxiliary">Category</h2>
            {{> molecules-tags }}
          {{/ singleCategory }}

          <h2 class="heading--auxiliary">Tags</h2>
          {{> molecules-tags }}

        </div>
      </div>

      <div class="l-sidebar">
        <div class="panel o-box">
          <h2 class="panel__title">People</h2>
          {{# peopleListShort }}
            {{> molecules-listing }}
          {{/ peopleListShort }}
        </div>
        <div class="panel o-box">
          <h2 class="panel__title">Upcoming Events</h2>
          {{> organisms-event-list }}
        </div>
        <div class="panel o-box">
          <h2 class="panel__title">Categories</h2>
          {{> molecules-category-filter }}
        </div>
      </div>

    </div>

  </main>
  {{> organisms-footer }}
</div>
